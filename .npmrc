# react-konva declares peer react@^19.3.0 while the project pins react 19.2.8.
# npm ci refuses that on a clean install (Vercel), so resolve peers the way the
# committed lockfile was built. Remove this once react/react-konva agree.
legacy-peer-deps=true
